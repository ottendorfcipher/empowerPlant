FROM rust:1.75 as builder

WORKDIR /app
COPY Cargo.toml ./
COPY src/ ./src/

RUN cargo build --release

FROM debian:bookworm-slim

RUN apt-get update && apt-get install -y \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /app/target/release/empower-plant-backend /usr/local/bin/empower-plant-backend

EXPOSE 8080

CMD ["empower-plant-backend"]
